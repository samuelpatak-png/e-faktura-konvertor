# Copy to .env and fill in real values. Never commit .env.

# Optional — error tracking (https://sentry.io). Leave blank to keep it fully inactive; no
# account is required to run the app. See src/instrument.ts.
VITE_SENTRY_DSN=
